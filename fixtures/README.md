# Fixtures

Fixtures are small repository snapshots used as automated scanner test inputs. They should stay focused on the source or configuration shape that a test needs and are not intended as tutorials.

Human-facing demonstrations belong in `examples/` instead. No examples are included yet.
