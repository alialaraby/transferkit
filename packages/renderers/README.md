# @transferkit/renderers

Human-readable handover and onboarding output renderers used by TransferKit.

This is a private development workspace. Its runtime code is bundled into [`@transferkit/cli`](https://github.com/alialaraby/transferkit) and it is not published as a standalone package.
