# @transferkit/cli

The TransferKit command-line interface for local-first software handover and onboarding workflows.

```bash
npm install --global @transferkit/cli
tk --help
```

The package is not published yet. See the [TransferKit repository](https://github.com/alialaraby/transferkit) for documentation and source installation instructions.
